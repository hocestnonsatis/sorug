[package]
name = "sorug-fuzz"
version = "0.0.0"
publish = false
edition = "2024"

# Standalone workspace so cargo-fuzz is not pulled into the parent members.
[workspace]

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
url = "2.5"

[dependencies.sorug]
path = ".."

[[bin]]
name = "url_fuzz"
path = "fuzz_targets/url_fuzz.rs"
test = false
doc = false
bench = false
